knitr::opts_chunk$set(echo = TRUE)
library(nlme)
fmla = distance ~ 0 + gender + age + gender*age
## REML (unstructured covariance)
fit.reml = gls(
fmla,
correlation = corCompSymm(form = ~ 1 | id),
weights = varIdent(form = ~ 1 ),
data = dental.long,
method = 'REML'
)
rep(nrow(dental.data)-length(beta.model),2)
